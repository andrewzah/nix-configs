#!/usr/bin/env bash

set -e

WLOG_FILE="$HOME/work/log.md"
fmt='+%y-%m-%d - %a - %H:%M'

[ ! -d "$HOME/work" ] && mkdir -p "$HOME/work"

printf "\n## %s\n\n" "$(date "$fmt")" >> "$WLOG_FILE"

exec "${EDITOR:-nvim}" "$WLOG_FILE"
